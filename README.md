# beyouart website

This repository contains the static website files for beyouart.

## Local preview

You can preview the site with any static file server, for example:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
